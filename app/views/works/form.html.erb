<%= form_for(@work) do |f| %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :plot %>
    <%= f.text_area :plot, class: 'form-control', rows: 4 %>
  </div>

  <div class="form-group">
    <%= f.label :picture %>
    <%= f.text_field :picture, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :release_date %>
    <%= f.date_select :release_date, class: 'form-control' %>
  </div>

  <div class="well">
    <h3>Artist</h3>
    <%= f.fields_for(:artist) do |d| %>
      <div class="form-group">
        <%= d.label :name %>
        <%= d.text_field :name, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= d.label :name %>
        <%= d.text_field :name, class: 'form-control' %>
      </div>

    <% end %>
  </div>

  <div class="well">
    <h3>Genres</h3>
    <%= f.fields_for(:genres) do |g| %>
      <div class="form-group">
        <%= g.label :name %>
        <%= g.text_field :name, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= r.label :artist %>
        <%= r.select :artist_id, Artist.all.map{|a| [a.name, a.id]}, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= g.label :destroy %>
        <%= g.check_box :_destroy, class: 'form-control' %>
      </div>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.submit "Save Work", class: 'btn btn-lg btn-success' %>
  </div>
<% end %>
